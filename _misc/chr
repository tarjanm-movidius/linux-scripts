#mount /dev/sda1 boot/
mount --bind / mnt/hd/
mount --bind /sys/ sys/
mount --bind /proc/ proc/
mount --bind /run/ run/
mount --bind /dev/ dev/
mount --bind /dev/shm dev/shm
chroot .
umount dev/shm dev run proc sys mnt/hd
